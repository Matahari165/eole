import { SessionConfigurator } from "@/components/session/session-configurator";

export const metadata = { title: "Nouvelle séance" };

export default function NewSessionPage() {
  return <SessionConfigurator />;
}
